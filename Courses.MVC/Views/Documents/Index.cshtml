@model IEnumerable<DocumentsViewModel>

@{
    ViewData["Title"] = "Index";
}


<div class="row">
    <div class="col-md-7">
        <div class="row">
            <div class="col">
                <h2>List of Images</h2>
            </div>
            <div class="col text-end">
                <a asp-controller="Documents" asp-action="Create" class="btn btn-warning">Upload Image</a>
            </div>
        </div>
        <div class="row">
            <table>
                <thead>
                    <tr>
                        <th>Image Name</th>&nbsp;
                        &nbsp;
                        &nbsp;

                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.ImageName</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>